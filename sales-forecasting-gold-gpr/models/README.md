# Models Directory

This directory will contain trained model files generated by the notebook:
- gold_xgb_model.pkl - Trained XGBoost model
- feature_info.pkl - Feature metadata and performance metrics

These files are generated when you run the Jupyter notebook.
